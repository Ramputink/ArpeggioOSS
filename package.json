{
  "name": "arpeggio-oss",
  "version": "0.1.0",
  "private": true,
  "description": "Arpeggio OSS \u2014 OMR backend demo: sheet music -> MusicXML -> internal model. Piano tutor pipeline (Phase 1).",
  "license": "AGPL-3.0-or-later",
  "workspaces": [
    "packages/musicxml-parser",
    "packages/omr-client",
    "packages/practice-engine",
    "packages/motor2-basicpitch",
    "packages/practice-web",
    "packages/song-library",
    "apps/web",
    "apps/learn"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm test --workspaces --if-present",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.65.0"
  }
}
