# Arpeggio OMR service — Python dependencies.
#
# Ranges (not exact pins) so the resolver can pick platform-compatible wheels.
# This matters for the native macOS 11 (Big Sur) backend, whose available
# OpenCV/NumPy wheels differ from the Debian Docker image. Requires Python 3.11+.

fastapi>=0.110,<1            # HTTP framework
uvicorn[standard]>=0.29      # ASGI server
python-multipart>=0.0.9      # multipart/form-data uploads for FastAPI

opencv-python-headless>=4.8,<5  # image preprocessing (no GUI deps)
numpy>=1.26,<3
PyMuPDF>=1.24                # PDF rasterize + split + rebuild clean PDF (imported as `fitz`)
