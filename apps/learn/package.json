{
  "name": "@arpeggio/learn",
  "version": "0.1.0",
  "private": true,
  "description": "Arpeggio Learn — the mobile-first learner app (PWA): a starter library of public-domain pieces with animated scrolling notation, an on-screen keyboard and microphone practice.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "share": "node serve.mjs",
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@arpeggio/musicxml-parser": "0.1.0",
    "@arpeggio/practice-engine": "0.1.0",
    "@arpeggio/practice-web": "0.1.0",
    "@arpeggio/song-library": "0.1.0",
    "@arpeggio/motor2-basicpitch": "0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.7.2",
    "tsx": "^4.19.2",
    "@types/node": "^20.17.0"
  }
}
